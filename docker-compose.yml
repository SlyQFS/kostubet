services:
  kostubet-github:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kostubet-github
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - kostubet_data:/data

volumes:
  kostubet_data:
